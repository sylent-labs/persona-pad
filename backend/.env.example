OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
# Optional. Cheaper/secondary model used only when the primary is rate limited (429).
OPENAI_FALLBACK_MODEL=
PERSONA_PAD_LOG_LEVEL=INFO
# Comma-separated list. In production, set to your deployed frontend origin(s).
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
